1. Reportes con graficos (Matplotlib)    
    representar visualmente resultados de analisis, como distribucion
    de codigo de estado HTTP

2. Servidor web para captura de logs (Flask)
    servidor simple que registre peticiones que recibe y las guarde
    en archivo log. Despues usar script para analizarlos.

Agregar a funcion general recopilacion de metodo y las veces que
recibe un codigo

ej:

metodos = {
    "GET": {"200": 5, "404": 6},
    "POST": {"200": 2, "500": 5}
}
